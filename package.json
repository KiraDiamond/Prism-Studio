{
  "name": "prism-studio",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "npm run build:ui && tauri build",
    "build:ui": "esbuild skinview-entry.js --bundle --format=esm --minify --outfile=ui/skinview.bundle.js",
    "build:portable": "npm run build:ui && tauri build --no-bundle",
    "check": "node scripts/check.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "esbuild": "^0.28.2"
  },
  "dependencies": {
    "skinview3d": "^3.4.2"
  }
}
